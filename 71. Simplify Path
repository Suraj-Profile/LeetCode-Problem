class Solution:
    def simplifyPath(self, path: str) -> str:
        stk = []
        for part in path.split("/"):
            if part == "..":
                if stk:
                    stk.pop()
            elif part and part != ".":
                stk.append(part)
        return "/" + "/".join(stk)
